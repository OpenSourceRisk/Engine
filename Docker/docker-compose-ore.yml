version: '3'

volumes:
    ccache_volume:

services:
  env_ore:
    image: env_ore:${ORE_TAG}
    environment:
      - CCACHE_DIR=/ccache
    volumes:
      - ccache_volume:ccache
    build:
      context: ../
      dockerfile: Docker/Dockerfile-ORE
      args:
        - quantlib_version=${QL_TAG}
        - num_cores=${NUM_CORES}
