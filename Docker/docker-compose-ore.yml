version: '3'

services:
  env_ore:
    image: env_ore
    build:
      context: ../
      dockerfile: Docker/Dockerfile-ORE
      args:
        - quantlib_version=1.18_d123ec8c7
        - num_cores=16
